name: CI Pipeline

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: frontend/package.json

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Run tests
        env:
          CI: true
        run: npm test -- --watch=false --passWithNoTests

      - name: Build production bundle
        env:
          CI: false
        run: npm run build

  backend:
    name: Backend Build & Test
    runs-on: ubuntu-latest
    needs: frontend
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: foodfast_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      SPRING_DATASOURCE_URL: jdbc:mysql://127.0.0.1:3306/foodfast_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      VNPAY_TMN_CODE: ${{ secrets.VNPAY_TMN_CODE }}
      VNPAY_HASH_SECRET: ${{ secrets.VNPAY_HASH_SECRET }}
      VNPAY_PAYMENT_ENDPOINT: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
      VNPAY_API_ENDPOINT: https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
      GPS_API_KEY: ${{ secrets.GPS_API_KEY || 'demo-gps-api-key' }}
      GPS_API_URL: https://maps.googleapis.com/maps/api
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install MySQL client
        run: sudo apt-get update && sudo apt-get install -y mysql-client

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Wait for MySQL to be ready
        run: |
          for i in {1..30}; do
            if mysql -h 127.0.0.1 -P 3306 -u root -proot -e "SELECT 1" &> /dev/null; then
              echo "MySQL is ready" && break
            fi
            echo "Waiting for MySQL..."
            sleep 5
          done

      - name: Run Maven tests
        working-directory: backend
        run: mvn -B test

      - name: Package Spring Boot application
        working-directory: backend
        run: mvn -B package -DskipTests

  integration:
    name: Integration Smoke Tests
    runs-on: ubuntu-latest
    needs: backend
    env:
      VNPAY_TMN_CODE: ${{ secrets.VNPAY_TMN_CODE || 'demo-tmn-code' }}
      VNPAY_HASH_SECRET: ${{ secrets.VNPAY_HASH_SECRET || 'demo-hash-secret' }}
      VNPAY_PAYMENT_ENDPOINT: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
      VNPAY_API_ENDPOINT: https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
      GPS_API_KEY: ${{ secrets.GPS_API_KEY || 'demo-gps-api-key' }}
      GPS_API_URL: https://maps.googleapis.com/maps/api
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify VNPay credentials are present
        run: |
          if [ -z "${VNPAY_TMN_CODE}" ] || [ -z "${VNPAY_HASH_SECRET}" ]; then
            echo "::warning::VNPay sandbox credentials are not configured."
            echo "::warning::Using demo card 9704198526191432198 (NGUYEN VAN A, exp 07/15, OTP 123456)."
          else
            echo "VNPay credentials detected."
          fi

      - name: Verify GPS API credentials are present
        run: |
          if [ -z "${{ secrets.GPS_API_KEY }}" ]; then
            echo "::warning::GPS API credentials are not configured; using demo key for limited smoke checks."
          else
            echo "GPS API credentials detected."
          fi

      - name: Backend artifact summary
        run: |
          echo "Listing backend build artifacts"
          ls -R backend/target
